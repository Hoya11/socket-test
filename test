; 1. 회원가입 후 로그인시 가족이 없어서 가족생성을 함 (이때 룸db도 같이 생성되고 join으로 해당룸에 입장함)
; => 로그아웃하고 다시 로그인할때는 가족생성 버튼을 누르지 않기때문에
; 로그인버튼으로 room DB find로 유저id를 비교해서 입장해야되나(?)

; ```jsx
; console.log("socket 연결");
;   io.use(async (socket, next) => {
;     const userInfo = socket.handshake.auth.user;
;     console.log("use부분", userInfo); // tnwjd
;     // 어딘가의 저장소에서 찾고있음
;     const session = await chatData.findOne({ userId: userInfo.userId });
;     console.log("세션", session);
;     const { userId, nickname, profileImage } = userInfo;
; ```

; 1. 가족 메인페이지에 입장이 된 경우 룸에도 입장이 되어있어야함
; => 되어있을경우 프로필 수정모달에서 가족 초대를 보낼경우 해당 유저에게 수락/거절 메세지가 오고 
; 해당 메세지를 수락 눌렀을경우 가족에 초대가되면서 room에 입장이 되야함



; 2. 해당 수락까지 잘 되서 룸에 가족 구성원들이 추가가 되었을 경우엔
; => 해당 유저들끼리 접속했을때 상태값을 추가해서 소켓아이디가 발행(?)되었을 경우 true값으로 변경해서 접속을 확인?

; => 사진, 음성, 일정 등록 시 가족들에게 알림이 가야됨 (해당 POST API에 소켓을 추가해야되나 싶음)


; [우리가족]
; 가족만들기







